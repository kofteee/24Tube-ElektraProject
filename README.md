Dockersız kullanım için iki ayrı terminalde:
cd frontend
npm start

cd backend 
npm start

Dockerlı kullanım için;
Frontend/server.js dosyasındaki localhost'ları backend yapınız.

Sonrasında:
docker-compose up --build  
      
