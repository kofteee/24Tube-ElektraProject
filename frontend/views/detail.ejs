<!DOCTYPE html>
<html lang="en">
<script>
window.onpageshow = function(event) {
  if (event.persisted) {
    window.location.reload(); // not sure
  }
};
</script>
<head><meta charset="UTF-8"><title><%= info.original_name %> Detayı</title></head>
<body>
  <h2>Dosya Detayı</h2>
  <ul>
    <li><strong>Ad:</strong> <%= info.original_name %></li>
    <li><strong>Boyut:</strong> <%= (info.size_bytes / 1024).toFixed(2) %> KB</li>
    <li><strong>Tarih:</strong> <%= new Date(info.uploaded_at).toLocaleString() %></li>
    <li><strong>Görüntülenme sayısı:</strong> <%= info.preview %></li>
    <li><strong>İndirilme sayısı:</strong> <%= info.download_number %></li>

    <form action="/tenant/download/confirm" method="post">
        <input type="hidden" name="tenant" value="<%= tenant %>">
        <input type="hidden" name="blob_key" value="<%= info.blob_key %>">
        <button type="submit">Download</button>
      </form>

    <form action="/tenant/download/history" method="post">
        <input type="hidden" name="tenant" value="<%= tenant %>">
        <input type="hidden" name="blob_key" value="<%= info.blob_key %>">
        <button type="submit">Download History</button>
      </form>

    <form action="/tenant/delete" method="post">
        <input type="hidden" name="tenant" value="<%= tenant %>">
        <input type="hidden" name="blob_key" value="<%= info.blob_key %>">
        <button type="submit">Delete</button>
      </form>
  
      <form action="/tenant/preview" method="post">
        <input type="hidden" name="tenant" value="<%= tenant %>">
        <input type="hidden" name="blob_key" value="<%= info.blob_key %>">
        <button type="submit">Preview</button>
      </form>
    
  </ul>
  <a href="javascript:history.back()">← Geri Dön</a>
</body>
</html>