<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dosya Önizleme</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      width: 100%;
      background-color: #000;
    }

    video, audio, iframe, img, object, embed {
      display: block;
      width: 100%;
      height: 100%;
      object-fit: contain;
      background-color: #000;
    }

    .fallback {
      color: white;
      text-align: center;
      padding-top: 2rem;
      font-family: sans-serif;
    }

    a {
      color: #4FC3F7;
      display: block;
      text-align: center;
      margin-top: 1rem;
      text-decoration: none;
      font-size: 1rem;
    }
  </style>
</head>
<body>

  <% if (ext === '.mp4') { %>
    <video controls autoplay preload="metadata" playsinline style="max-width: 100%;">
      <source src="<%= url %>" type="video/mp4">
      Tarayıcınız video etiketini desteklemiyor.
    </video>

  <% } else if (ext === '.mp3') { %>
    <audio controls autoplay>
      <source src="<%= url %>" type="audio/mpeg">
      Tarayıcınız ses oynatmayı desteklemiyor.
    </audio>

  <% } else if (ext === '.pdf') { %>
    <iframe src="<%= url %>" frameborder="0"></iframe>

  <% } else if (ext === '.png' || ext === '.jpg' || ext === '.jpeg' || ext === '.gif' || ext === '.webp' || ext === '.svg') { %>
    <img src="<%= url %>" alt="Görsel Önizleme">

  <% } else if (ext === '.txt') { %>
    <iframe src="<%= url %>" frameborder="0"></iframe>

  <% } else if (ext === '.html' || ext === '.htm') { %>
    <iframe src="<%= url %>" frameborder="0"></iframe>

  <% } else if (ext === '.docx' || ext === '.xlsx' || ext === '.pptx') { %>
    <iframe src="https://view.officeapps.live.com/op/embed.aspx?src=<%= encodeURIComponent(url) %>" frameborder="0"></iframe>

  <% } else { %>
    <div class="fallback">
      <p>Bu dosya türü tarayıcıda önizlenemiyor.</p>
      <a href="<%= url %>" download>Dosyayı indir</a>
    </div>
  <% } %>

  <a href="javascript:history.back()">← Geri Dön</a>
</body>
</html>